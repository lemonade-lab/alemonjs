# 工程模板

https://github.com/lemonade-lab/alemonjs

```sh
npm create alemonjs@latest -y
```

默认创建 `alemonjs` 模板。

如果要创建 `alemonb` bot 模板：

```sh
npm create alemonjs@latest -- --bot
```

也支持：

```sh
npm create alemonjs@latest -y --bot
```
