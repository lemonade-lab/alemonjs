# 是否启用无头模式
headless: 'new'
# 超时时间
timeout: 30000
# 启动命令行参数
args:
  [
    '--disable-gpu',
    '--disable-dev-shm-usage',
    '--disable-setuid-sandbox',
    '--no-first-run',
    '--no-sandbox',
    '--no-zygote',
    '--single-process'
  ]
# Chrome 浏览器可执行文件的路径
# chromePath: ''
